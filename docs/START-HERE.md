# modu 새 Claude Code 창 — 시작 문서

> 이 프로젝트는 `dalgme/restart`(재기지원사업 플랫폼)를 복제해 **모두의창업** 용으로 개조하는 작업입니다.
> 원래 restart 세션에서 기획·인프라 구축까지 진행했고, **여기서부터 modu 전용 창으로 분리**합니다.

---

## 1. 지금까지 된 것 (2026-09-07)

| 항목 | 상태 |
|---|---|
| GitHub | ✅ `dalgme/modu` — restart 코드 전체 복제 푸시 완료 |
| Supabase | ✅ 프로젝트 `modu` (`osrigknfrzsqjrgihgao`, ap-northeast-2) — 테이블 27개 · 버킷 3개 · RLS 전부 활성 |
| 도메인 정의 | ✅ 역할·데이터모델·정산요건 확정 → `CLAUDE.md` |
| Vercel | ❌ **미생성** |
| 초기 계정 부트스트랩 | ❌ **미실행** (`/api/setup`) |
| 코드 개조 | ❌ **착수 전** — 아직 restart 코드 그대로 |

### Supabase 마이그레이션 적용 현황
- **적용**: 0001·0002·0003·0005(기반) / 0009·0017·0035·0038·0030(필드) / 0014·0041(상태값) /
  0028·0031·0032·0034(문의·OTP·FAQ·운영요청) / 0036·0037·0039·0044·0045(게시판·문자예약·보완요청·수정권한·단일본인덱스)
- **건너뜀 — 재기지원 전용 데이터라 넣으면 오염됨**:
  - `0004_seed.sql` 지원유형 시드(경영개선·폐업정리) → **사업그룹 확정 후 새로 작성**
  - 서식 21종(0006·0007·0008·0010~0013·0015·0016·0018~0027·0029·0040, 약 221KB) → 모두의창업 서식 새로 제작
  - `0032` FAQ 시드 4건 (표 구조만 적용)
  - `0037` 주간 안내문구는 **사업 중립 문구로 대체**하고 `mentor_weekly_reminder_enabled='false'` 로 꺼 둠
  - `0033·0042·0043` 기존 운영데이터 정정용 → 빈 DB 에선 무의미

> ⚠ 마이그레이션 파일은 리포에 **원본 그대로** 남아 있습니다. 새 환경에 재적용할 일이 생기면
> 위 "건너뜀" 목록을 그대로 다시 건너뛰세요.

---

## 2. 다음 창에서 맨 처음 붙여넣을 프롬프트

```
이 리포는 dalgme/restart 플랫폼을 복제해 "모두의창업" 용으로 개조하는 프로젝트입니다.
CLAUDE.md, docs/START-HERE.md, docs/PLATFORM-CLONE-HANDOVER.md, docs/DOMAIN-REMODEL-GUIDE.md
를 먼저 읽고 시작하세요.

작업 원칙:
- 추측하지 말고 실제 파일을 읽고 판단할 것
- 코드 변경 후 npm run typecheck / lint / build 3종을 모두 통과시킬 것
- 상태머신(case_status)이나 doc_key 를 바꿀 때는 UI 게이트와 서버 액션 게이트를 반드시 함께 수정할 것
- 금액 집계(정산) 로직은 화면 표시액과 확정 저장액이 같은 함수에서 나오게 할 것

첫 작업: CLAUDE.md §3(진행 단계)과 §4(서류 체계)가 아직 미확정입니다.
현재 restart 코드의 case_status 전이 지점과 doc_key 사용처를 전수 조사해
docs/CURRENT-STATE.md 로 정리해 주세요. 코드는 아직 바꾸지 마세요.
```

---

## 3. 사용자에게 아직 물어봐야 하는 것 (착수 전 확정 필요)

1. **진행 단계에 지급/승인 단계가 필요한가?**
   원본 11단계는 보조금 지급 절차. 모두의창업 축은
   `그룹 개설 → 멘티 등록 → 멘토 배정 → 컨설팅 1~4회 → 평가서 → 종료` 로 보이는데,
   센터의 **승인 게이트**가 실제로 있는지 확인 필요.
2. **평가서(`evaluation_report`)는 멘티당 1건인가, 회차마다인가?**
   단일본이면 `0045` 유니크 인덱스 조건에 반드시 추가해야 함
   (원본에서 이걸 안 걸어 보고서가 5건까지 쌓인 사고가 있었음).
3. **컨설팅 유형(mode)의 정확한 목록과 단가**, 그리고 **사업그룹별로 단가가 다른지.**
4. **정산 주기** — 월 단위인가, 사업그룹 종료 시점인가, 회차 완료 즉시인가.
5. **정산 인정 기준** — 회차를 '완료'로 치는 조건(보고서 등록? 운영진 승인?).
6. **사업그룹 3~5개의 실제 이름과 승계 관계** (어느 그룹 → 어느 그룹).

---

## 4. 인프라 마무리 (Vercel)

`docs/PLATFORM-CLONE-HANDOVER.md` 3절의 환경변수 27개를 채워야 합니다. 최소 세트:

- `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY`
- `SETUP_TOKEN` (초기 계정 부트스트랩 `/api/setup` 1회용)
- `CRON_SECRET`, 알림(Solapi·SMTP) 키 — 문자 발송 붙이기 전까지는 비워도 동작
- 리전은 **icn1** 고정 (PDF Chromium 콜드스타트)

> 값은 **절대 리포에 커밋하지 말 것.**
