# 모두의창업 (modu) — Claude 세션 메모리

> 이 파일은 새 Claude Code 세션이 시작될 때 자동으로 읽힙니다.
> **원본 뼈대**: `dalgme/restart` (재기지원사업 운영관리 플랫폼)에서 복제 — 인프라·인증·문서·알림·PDF·감사 구조를 승계.
> 복제 절차는 `docs/PLATFORM-CLONE-HANDOVER.md`, 개조 지점은 `docs/DOMAIN-REMODEL-GUIDE.md` 참조.

---

## 0. 이 프로젝트는 무엇인가

- **사업명**: 모두의창업
- **발주·승인**: 세종창조경제혁신센터
- **운영 총괄**: (주)렛츠
- **한 줄 정의**: 모두의창업 참여 **멘티와 멘토를 연결·배정**하고, **멘토링 운영과정을 관리**하는 플랫폼.

### 주체별 목적
| 주체 | 이 시스템에서 얻는 것 |
|---|---|
| 세종창조경제혁신센터 (발주) | 멘토별·멘티별 **실시간 진행현황** 확인 |
| (주)렛츠 (운영 총괄) | 기수별 멘티그룹당 **개별멘토 배정**, **중간 멘토 변경**, **멘토링 날짜·보고서 첨부파일 등록**, **평가서 첨부파일 등록** 지원·관리 |

---

## 1. 역할

| 역할 키(예정) | 화면 표기 | 하는 일 | 원본 대응 |
|---|---|---|---|
| `institution` | 세종창조경제혁신센터 | 진행현황 열람(발주·승인) | institution(진흥원) |
| `operator` | (주)렛츠 | 운영 총괄 — 배정·변경·등록 관리 | nextlab(넥스트랩) |
| `staff` | (주)렛츠 실무자 | 현장 실무 | mentor |
| `<미정>` | **대상자 — 미정** | — | mentee |

### ⚠ 미해결 (착수 전 반드시 확정)
1. **`대상자` 역할이 비어 있음.** 문맥상 **멘티**로 보이나 명시되지 않음.
2. **`멘토`가 역할 목록에 없음.** 사업 정의의 핵심은 "멘토 배정"인데,
   현장 실무가 "(주)렛츠 개별 실무자"로 매핑되어 있음.
   → **멘토 = 렛츠 실무자**인가, 아니면 **외부 멘토가 별도의 5번째 역할**인가?
   이 답에 따라 `user_role` enum 값과 라우트 그룹 수가 달라진다.

---

## 2. 데이터 모델 — ⚠ 원본과 구조가 다름 (가장 중요)

원본 restart는 **1:1** 구조다.
```
case (멘티기업 1곳) ── mentor_assignment (활성 멘토 1명)
```

모두의창업은 **기수 → 그룹 → 그룹당 멘토 1명 : 멘티 N명** 구조로 보인다.
```
cohort (기수)
  └ group (멘티그룹)
       ├ mentor_assignment (그룹당 개별멘토 1명, 중간 변경 가능)
       └ group_members (멘티 N명)
            └ mentoring_session (날짜) → 보고서 첨부 / 평가서 첨부
```

**따라서 단순 리네이밍으로 끝나지 않는다.** 최소한 아래가 필요하다.
- `cohorts` 신설 (기수)
- `groups` + `group_members` 신설 (멘티그룹 · 소속 멘티)
- `mentor_assignments` 를 **case 단위 → group 단위**로 변경 (유니크 인덱스도 함께)
- `mentoring_logs` 를 **그룹 단위 세션**으로 재해석 + 평가서 doc_key 추가
- 진행현황을 **멘토별 / 멘티별** 두 축으로 제공 (원본의 mentor-board / mentee-board 재사용 가능)

> **착수 전 확정 필요**: 멘티는 개인인가 팀(창업팀)인가? 그룹당 멘티 수 상한은? 기수마다 재배정되는가?

---

## 3. 진행 단계(상태머신) — 미정

원본의 11단계(등록→배정→연락→일지→신청서→검수→승인→통보→지급)는
**보조금 지급 절차**라 모두의창업에 그대로 맞지 않을 가능성이 높다.

모두의창업의 축은 대략 이렇다(확정 필요).
```
기수 개설 → 멘티 모집/등록 → 그룹 편성 → 멘토 배정
  → 멘토링 진행(N회) → 회차별 보고서 등록 → 평가서 등록 → 기수 종료
```

**⚠ 불변 규칙**: 화면 버튼의 활성 조건과 서버 액션의 상태 게이트는 **반드시 같이** 수정한다.
어긋나면 "버튼은 눌리는데 실패"가 난다 (원본에서 실제 발생한 버그).

---

## 4. 서류 체계 (doc_key) — 미정

확정된 것만 적는다.

| doc_key(예정) | 이름 | 성격 |
|---|---|---|
| `mentoring_report` | 멘토링 보고서 | 누적(회차별) |
| `evaluation_report` | 평가서 | **신규** — 단일본/누적 미정 |

- **단일본**은 `supabase/migrations/00XX_documents_singleton_doc_keys.sql` 유니크 인덱스 조건에 반드시 추가.
- 업로드 UI는 `CaseDocUpload` 재사용.

---

## 5. 기술 스택 (원본 승계)

Next.js 14.2 App Router / TypeScript / Supabase(Postgres·Auth·Storage·RLS) /
Tailwind + shadcn(Radix) / react-hook-form + zod / playwright-core + @sparticuz/chromium(PDF) /
pdf-lib(병합) / Solapi(SMS) / nodemailer(이메일) / Vercel(icn1)

---

## 6. 반드시 지킬 규칙 (원본에서 사고가 났던 지점)

1. **신원 분리** — 스태프 특권·감사 실행자는 `getRealSessionProfile()`, 업무 명의는 `getSessionProfile()`.
   대행(view-as) 중 이 둘이 달라진다.
2. **RLS를 범위 강제에 쓰지 말 것** — service_role 경로에서는 RLS가 적용되지 않고,
   대행 중에는 `auth.uid()`가 실행자라 범위가 넓어진다. 배정 확인은 **코드에서 직접**.
3. **`audit_logs`는 `actor_id = auth.uid()` 강제** — 남의 명의로 남기려면 service_role 경로.
   아니면 **조용히 유실**된다. insert 에러를 절대 삼키지 말 것.
4. **PDF 라우트는 `export const maxDuration = 60`** — 서버리스 Chromium 콜드스타트.
5. **문자 발송은 try/catch로 격리** — 문자 실패가 본 작업을 막으면 안 된다.
6. **`NEXT_PUBLIC_*` 변경 시 빌드 캐시 없이 재배포.**
7. **서버 액션에서 역할 불일치 시 `redirect()` 금지** — 페이지 전체가 튕긴다. `{ ok:false, error }` 반환.
8. **단일본 doc_key는 DB 유니크 인덱스로 강제** — 앱 코드만 믿으면 중복이 쌓인다(원본에서 5건까지 쌓인 사고).

---

## 7. 개조 순서 (이 순서를 지킬 것)

```
1) 역할 확정        ← 현재 2건 미해결 (§1)
2) 데이터 모델 확정  ← 기수/그룹 구조 (§2) — 원본과 다름, 가장 중요
3) 단계 확정        ← 상태머신 (§3)
4) 서류 목록 확정    ← doc_key (§4)
5) 화면 배선
6) PDF 서식 / 알림 문구 / 브랜딩 교체
7) 역할별 권한 격리 점검
```

> **1~4를 확정하기 전에 화면부터 만들지 말 것.** 데이터 모델과 상태머신이 바뀌면 화면을 다시 짜야 한다.

---

## 8. 브랜딩 치환 대상 (원본 잔재)

```bash
grep -rn "재기지원\|진흥원\|넥스트랩\|대전\|restart.poclab.kr" src/ | wc -l
```
전수 치환 후 0건이 되어야 한다.

---

## 9. 작업 브랜치 / 배포

- 기본 작업 브랜치: `<미정>`
- 배포: main 머지 → Vercel 자동 배포
- 검증 3종: `npm run typecheck` · `npm run lint` · `npm run build` (모두 통과해야 머지)

---

## 10. 결정 기록

> 결정이 바뀌면 덮어쓰지 말고 날짜와 함께 추가.

- 2026-09-03 원본 `dalgme/restart` (커밋 `5ca3db0`) 에서 복제. 슬러그 `modu`.
- 2026-09-03 역할 3개 확정(세종창조경제혁신센터 / (주)렛츠 / (주)렛츠 실무자). **대상자·멘토 역할 미확정.**
